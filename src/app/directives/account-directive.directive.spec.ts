import { AccountDirectiveDirective } from './account-directive.directive';

describe('AccountDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new AccountDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
